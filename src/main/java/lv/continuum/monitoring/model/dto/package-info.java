/**
 * Data transfer objects (DTOs) that are exposed via REST API.
 * <p>
 * Having DTOs separate from entities ensures a more fine-grained control
 * over what data is exposed and how it is validated.
 */
package lv.continuum.monitoring.model.dto;
